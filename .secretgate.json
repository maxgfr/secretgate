{
  "//": "secretgate's own project allowlist (dogfood): tests hold FAKE secret-shaped fixtures by design. The rule-database files (rules/, rules.gen.ts, the bundles) need no entry — the engine never flags its own detection patterns.",
  "allowlist": {
    "paths": [
      "tests/**"
    ]
  }
}
